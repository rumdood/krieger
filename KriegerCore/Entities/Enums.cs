using System;

namespace Krieger
{
    public enum PlayerColor
    {
        White,
        Black
    }

    public enum MoveDirection
    {
        Horizontal,
        Vertical,
        Diagonal,
        Strange
    }
}
